// for installing npm i package-name
// for installing specific version npm i package-name@version

// 1.0.0 major-minor(added functions)-patches(corrected errors)

// nodemon automatically rerun the file npm i package-name
//--save-dev only needed in canpm i package-namese of dev not in time of deployment
// --global for installing for global

// ~ accepting patches done to the specified version
// > latest version accepting only versions above specified

// npm view nodemon version